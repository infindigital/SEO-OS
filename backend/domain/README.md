# backend/domain

Enterprise business rules: entities, value objects, and domain services that
model the SEO problem space (sites, pages, keywords, audits, rankings).

Pure TypeScript with **no dependencies** on frameworks, databases, or I/O. This
is the innermost layer — nothing here imports from `application/`,
`infrastructure/`, or `interface/`.
