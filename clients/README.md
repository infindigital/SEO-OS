# clients

Per-client workspace configuration for the SEO OS. Each client onboarded to
the platform is represented here by its own directory containing that client's
settings, site definitions, target keywords, and integration configuration.

This directory holds **configuration and data descriptors** — not application
logic. Business rules that operate on clients live in `backend/`.
