# crawler/config

Crawl configuration inputs (data, not code): start URLs, `maxPages`, `maxDepth`,
and per-client overrides. The scheduled weekly crawl reads its site list from
`automations/scheduled-crawls.json`.
