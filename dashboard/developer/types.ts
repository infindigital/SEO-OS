/** A developer that a task can be assigned to. */
export interface DeveloperOption {
  id: string;
  label: string;
}

/** A client a task can be associated with. */
export interface ClientOption {
  id: string;
  label: string;
}
