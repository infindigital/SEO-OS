# docs

Documentation for the Infin Digital SEO OS.

- `architecture/` — system architecture, clean-architecture layers, folder map
- `database/` — data-model overview (tables per module)
- `api/` — API and endpoint conventions
- `modules/` — one folder per product module (15)

The authoritative product specification is [`../PROJECT_SPEC.md`](../PROJECT_SPEC.md).
