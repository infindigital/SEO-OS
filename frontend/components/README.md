# frontend/components

Shared, reusable UI components and the design system, composed from the
shadcn/ui primitives in `src/components/ui`. Feature-specific components live
with their feature module in `dashboard/`.
