# templates/reports

Report layout templates (client SEO reports, audit summaries). The audit
Markdown renderer lives in `backend/infrastructure/audit`; reusable, data-driven
report templates live here.
